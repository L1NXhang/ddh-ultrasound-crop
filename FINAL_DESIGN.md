# DDH超声自动裁剪模型最终方案

多设备截图与无患者信息条件下的算法设计和实现说明

版本 2.0　适用数据规模 1000～2000 张　交付状态 研究代码与训练方案

最终决策：采用单类别窗口检测器，加共享 ResNet18 的上下边界预测模型；增加安全包络辅助任务与切面可用性质控。不同 Graf 类型共用模型，不把分型正确作为裁剪前提。全部原图保留，现阶段所有裁剪建议必须经人工确认。

本方案解决的是训练算法、标注协议和可重复验证，不包括临床诊断。随包没有经过 DDH 数据训练的权重，也没有真实数据准确率。代码已做合成数据测试；这与医学有效性验证是两件不同的事。

## 1 必要澄清问题

以下问题会影响训练。现有信息不足时按本节假设继续执行，不再等待回答。

1. 是否全部为单幅、单窗口静态超声截图，还是有双屏、拼图、视频？
2. 人工结果是否保留原截图坐标？是否曾缩放、旋转或重新压缩？
3. 是否能够补标每张图的超声窗口，并在一部分图上标出必须保留的完整结构范围？
4. 是否有非标准切面、严重模糊、遮挡等明确应当交给人工的负例？
5. 能否获得可靠的采集批次或视频来源线索？没有患者信息，不等于一定没有这些线索。
6. 图像上的测量线、文字或刻度是否压在有效成像窗口内部？
7. 老师认可的“安全”是保留哪些结构和上下文？允许边界误差多大？
8. 今后能否单独采集一批可追踪检查编号的新数据，做真正独立的验证？

执行假设：当前输入是单窗口截图；可以补标窗口框；能回溯人工裁剪在原图中的位置；只能可靠识别部分界面样式，不能据此猜医院、设备型号或患者身份；切面标准与必保结构由老师制定。暂定边界误差容限为窗口高度的 2%，它是工程起点，不是医学标准。

重要约束变更：现有数据完全没有患者标识，因此无法满足“必须按患者划分”的原始要求。近重复分组只能降低泄漏，不能恢复患者独立性。当前测试只能称为“经相似图组隔离的探索性评估”。不能改用随机图片划分后仍宣称患者级测试，也不能根据图像内容虚构患者编号。

## 2 任务数学化定义

原始截图为 I，尺寸为 W₀ × H₀。步骤 A 输出超声窗口 B=(x₀,y₀,x₁,y₁)，宽 W=x₁−x₀，高 H=y₁−y₀。坐标均为原截图像素，右边界和下边界不包含在区域内。

步骤 B 输出窗口局部的两个归一化坐标。最终裁剪宽度等于检测窗口宽度；步骤 B 不再裁左右。

$$r_t=y_t/H,\quad r_b=y_b/H,\quad 0\le r_t<r_b\le1$$

$$C=(x_0,\lfloor y_0+Hr_t\rfloor,x_1,\lceil y_0+Hr_b\rceil)$$

专家提供两种不同标签：偏好裁剪区间 g，和安全包络 S。S 是所有必须保留的结构与必要上下文的联合外接矩形，而不是“目标病灶框”。步骤 B 用 S 的纵向范围学习保留约束，端到端评估用完整 S 检查左右方向是否被步骤 A 误裁。

优化目标是尽可能接近人工偏好，同时惩罚向内切入 S 的预测。现有有限样本不可能证明“关键结构绝不被裁掉”；实际实现为保守预测、拒绝高风险样本、原图不覆盖、人工确认。

为什么不是圆或椭圆：它们既与实际的全宽矩形裁剪操作不一致，也容易去掉测量参照结构。把解剖形状分成多类再套裁剪模板，还会引入分类错误传播。

## 3 基线方案

步骤 A 的选择已经由多设备条件决定：主方案使用单类别目标检测，不用全图非黑像素外接框。有效超声中的低回声和无回声区域必须保留；文字、刻度和体位图也可能是亮像素，因此亮度阈值不能定义窗口。

如果日后某个数据子集确证来自同一个固定界面，可使用人工核验过的归一化窗口模板作为该子集的对照；界面布局、截图缩放方式或视口改变就必须拒绝模板。当前来源混杂，不能把模板作为总体主方案。

选择检测而非分割的理由：目前要输出矩形成像窗口，窗口框标注成本低，目标数量通常为一。若以后要求去除扇形扫描视场外区域或处理不规则拼图，才需要单独的视场掩膜；那是任务扩展，本包不实现。

步骤 B 最小基线：ImageNet 预训练 ResNet18，全局平均池化得到 512 维特征，经 512→128→3 的 MLP 输出三个有序间隔，训练 Smooth L1 加纵向 IoU 损失。不训练辅助头，不按 Graf 分模型。运行 train --stage crop --baseline 即可。

另设固定裁剪对照：仅在训练组中计算人工 top 和 bottom 的中位数，将这两个常数用于所有测试图。不能用全量数据计算中位数。主指标既报告真窗口下的步骤 B，也报告预测窗口下的整个流程。

此基线足以检验“学习模型是否比固定位置更好”，但人工裁剪框本身并不能告诉模型哪些结构不可缺失。不能把它包装成具备解剖安全保证的模型。

## 4 最终推荐模型

### 4.1 窗口检测

模型为 torchvision Faster R-CNN MobileNetV3 Large FPN，COCO 预训练。保留 RPN 和 FPN，把最后的检测分类器改成背景与窗口两类。输入为 RGB 原截图；内部等比例缩放，推荐短边 640、长边上限 960。模型输出坐标由 torchvision 还原到原截图尺寸。[1][2]

选取分数至少 0.8 的唯一窗口；没有窗口、多于一个高分窗口，或者窗口任一边小于 32 像素时拒绝。0.8 是初始筛选值，不是“80% 安全”的概率。对真正的双屏图，不自动猜要处理哪一侧。

### 4.2 上下边界与辅助任务

输入窗口转换为灰度，再复制成 3 通道；按比例缩放并居中填充到 384 × 384，使用 ImageNet 均值与标准差。保留真实内容在填充画布上的 ROI。绝不把长方形窗口直接拉伸为正方形。

主干使用 ImageNet 预训练 ResNet18。小样本训练时固定 BatchNorm 的运行均值和方差，仍训练卷积及仿射参数。第三残差阶段输出步长 16 的空间特征；以 ROIAlign 取回真实内容区域，避免填充宽度直接决定裁剪结果。

空间头先做 256→64 的 1×1 卷积，再对 ROIAlign 的 24×16 特征沿水平方向分别求均值和最大值，拼成 128×24。两层一维卷积 128→64→32，kernel=3、padding=1、ReLU；展平为 768，经 768→128→3，使用 ReLU 和 Dropout 0.2。这样保留纵向位置，比只做全局池化更贴合本任务。

安全包络头共享 768 维纵向特征，经 768→64→3，预测专家安全包络的纵向范围。它不是解剖分割，也不是关键点定位；只作为低标注成本的结构范围辅助任务。质控头取最后残差阶段的 512 维全局特征，用 Linear(512,1) 预测本图是否适合自动提出裁剪建议。

| 张量 | 形状 | 含义 |
| --- | --- | --- |
| image | N × 3 × 384 × 384 | 灰度三通道及等比填充 |
| roi | N × 4 | 填充画布中真实窗口的 xyxy |
| bounds | N × 2 | 窗口内归一化 top 和 bottom |
| safe | N × 2 | 辅助预测的必保纵向范围 |
| quality | N | 可用性 logit，未做 sigmoid |
| embedding | N × 512 | L2 归一化特征，用于异常距离筛查 |

### 4.3 有序坐标参数化

预测三个间隔：上方空白、保留区域、下方空白。对三个 logits 做 softmax，并加很小的正下限，避免极端浮点值导致上下边界重合。

$$p_i=(1-3\epsilon)\operatorname{softmax}(z)_i+\epsilon,\quad\epsilon=10^{-4}$$

$$r_t=p_0,\qquad r_b=p_0+p_1$$

上下边界天然有序。不需训练后排序，也不会把两条相互独立的回归值交换。安全头使用相同参数化。

最终选择是“空间边界回归加安全包络与质控的多任务模型”。步骤 B 不选通用检测器，因为横坐标固定；不选全像素分割，因为现有标签不支持，也增加标注成本；不强制增加多个精确关键点，因为当前只是纵向裁剪。若将来进入角度测量任务，再定义真正的解剖点和结构分割，不能把现有 safe 输出当作关键点。[3]

## 5 数据与标签设计

### 5.1 必需标签与标注规则

统一使用 UTF-8 JSONL，每行一图。全部坐标以原始解码截图为准。程序不自动按 EXIF 旋转图片，以免与标签不一致。

| 字段 | 约定 | 是否必需 |
| --- | --- | --- |
| id 和 path | 唯一图片编号和相对图片路径 | 必需 |
| window | 原图 xyxy 整数窗口框 | 每图必需 |
| crop | 原图 top 和 bottom；不可用图为 null | 裁剪监督所需 |
| safe_box | 所有必保结构联合 xyxy，含专家约定上下文 | 推荐补标 |
| quality | 1 为可提出裁剪建议，0 为应拒绝，null 为未知 | 质控所需 |
| style_group | 人工核实的界面外观组；未知为 unknown | 推荐 |
| source_group | 可靠的同一序列或采集批次；未知为空 | 可选 |
| leak_group 和 split | 审核后的相似组及四份划分 | 划分后必需 |
| graf_type | 已有分型；不得推断填充 | 可选 |
| human_modified | 同一模型预测是否被人工修改，0 或 1 | 后续评估可选 |

只有人工裁剪图但没有边界时，先确认它没有缩放、旋转、加边。可用匹配算法辅助寻找原图位置，但必须人工核对；多处相似或匹配不唯一时重新标注，不能默默使用匹配结果。窗口框和安全包络不是自动从裁剪标签推导出来的独立真值。

先标 50 张覆盖不同外观和质量的图，由老师统一规则。再补标全部 1000～2000 张的 window；训练集先补 200～300 张 safe_box，覆盖位置、质量、异常外观，不只选择典型好图。val、calib、test 尽可能全部补安全与质控标签，否则必须报告未标注数。负例应来源于真实非标准或质量不足图；若没有负例，不声称质控头可用。

两位标注者独立复核至少一部分数据。偏好 crop 分歧先看是否均包含必保范围；保留标注者原始版本，经裁定得到单一训练标签。不要把边界取平均后意外裁掉某个标注者认为必须保留的结构。无法确定必保范围的图标为人工处理，而不是强制形成精确标签。

代码使用裁定后的单标签，没有实现多标注者软分布学习。额外推理边距默认每侧 0.02H，需按验证结果选定并冻结。safe_box 已包含专家上下文，额外 0.02H 是对预测误差的工程补偿，不应把两者混为同一个边距。

### 5.2 没有患者信息时的划分

首先计算解码图像精确 SHA256；再在人工窗口内比较 64 位差分哈希与 32×32 灰度归一化相关。默认哈希距离不超过 4 且相关不小于 0.98 的图对是近重复候选。已核实的 source_group 也合并到同一连通分量。

人工检查 pairs.json；误合并需要拆分，漏掉的同序列图需要合并。大连通分量特别检查链式误合并。该方法会漏掉探头移动、缩放、左右侧不同的同患者图；它并不是患者识别器。文件名、同一天或同一目录，未核实时不得充当患者或序列证据。

审核后以整个 leak_group 分配约 65% train、15% val、10% calib、10% test。比例按组数，图片数可不同。没有足够组数时程序报错，不自动改用逐图划分。正式实验冻结清单、seed 和文件哈希；所有模型共享同一划分。

若界面样式足够多，额外执行留一种 style_group 外测，且整组移动，不能拆散已有 leak_group。未知真实医院时，只能称“外观样式外测”，不能称“跨医院验证”。只有以后获得独立采集、可追踪的新测试数据，才有条件评价患者级泛化。

## 6 损失函数

对有有效 crop 的图计算回归和区间重叠；对有 safe_box 的可用图计算保留和辅助包络；对 quality 已标注的图计算质控。缺失标签使用布尔 mask，不填成负例。每项按本项有标签的样本均值计算。

$$L=L_{reg}+0.25L_{IoU}+2L_{keep}+0.25L_{safe}+0.25L_{quality}$$

$$L_{keep}=\operatorname{mean}\left([r_t-s_t]_+ + [s_b-r_b]_+\right)$$

其中 s 是专家 safe_box 转换到窗口内的纵向范围。保留惩罚是单侧的：裁进安全包络才受罚，不把外扩与内切视为同等错误。

| 损失 | 具体实现 | 初始权重 |
| --- | --- | --- |
| Lreg | 两个坐标 Smooth L1，beta=0.02 | 1 |
| LIoU | 1 减纵向区间 IoU | 0.25 |
| Lkeep | 超过安全上界或未达到安全下界的 ReLU | 2 |
| Lsafe | 辅助安全区间 Smooth L1，beta=0.02 | 0.25 |
| Lquality | binary_cross_entropy_with_logits | 0.25 |

不另加 GIoU：一维回归本身在不重叠时仍有梯度。没有语义掩膜，所以不使用像素边界损失或 Dice。没有训练异方差方差头，因此没有所谓“不确定性损失”；推理不确定性来自 MC Dropout。上述权重是可训练的初值，必须做验证和消融，不是已被本数据证明的最优超参数。

步骤 A 使用 torchvision 检测器的标准 RPN objectness、RPN box regression、ROI classification 和 ROI box regression 四项损失等权相加。它与步骤 B 分开训练，不进行脆弱的端到端联合微调。

只有 crop 框时，Lkeep 与 Lsafe 都关闭。模型仍能学习人工偏好，但无法区分“安全的宽裁剪”和“漏结构但看起来像多数标签的窄裁剪”。补标必保范围是安全目标有直接监督的必要步骤之一，而不是可由网络结构替代的承诺。

## 7 训练方案

### 7.1 可执行配置

| 项目 | 步骤 A | 步骤 B |
| --- | --- | --- |
| 推荐尺寸 | 短边 640，长边不超过 960 | 384 × 384 等比填充 |
| 初始化 | COCO 检测预训练 | ImageNet ResNet18 |
| 优化器 | AdamW | AdamW |
| 学习率 | 全模型 1e-4 | 主干 3e-5，其他层 3e-4 |
| weight decay | 1e-4 | 1e-4 |
| batch size | 2 | 8 |
| 最大 epoch | 40 | 80 |
| 学习率策略 | 余弦下降，无 warmup | 余弦下降，无 warmup |
| 梯度裁剪 | norm 上限 5 | norm 上限 5 |
| 早停 | val 连续 8 轮不改善 | val 连续 12 轮不改善 |

代码默认 FP32。8～16GB 级单卡可作为起点，实际显存取决于截图纵横比和运行环境；显存不足先降低 batch，而不是损失性压缩原始标签。初次安装需要下载预训练权重；包内不含权重，也不假装已经训练完成。

步骤 A 最佳检查点按验证窗口的平均 1−IoU 选择；漏检记为零 IoU。步骤 B 按带标签 mask 的复合验证损失选择，当前实现按 batch 样本数汇总。两者都只看 val，不看 calib 或 test。安全包络评估若不达要求，不得因复合损失最低而启用快速确认。

### 7.2 增强和困难样本

代码已实现步骤 A 小幅亮度变化与标签同步的左右镜像；步骤 B 亮度和对比度各 0.9～1.1 倍、水平镜像、小幅乘性噪声。只有纵向裁剪任务且不预测左右侧时，水平镜像不改变纵向标签。原有 laterality 字段不用于模型目标；未来若预测侧别，镜像后必须同步变更侧别或禁用。

禁止上下翻转、大角度旋转、弹性形变、任意裁去原图内容、强对比度改变和擦除关键结构。当前不使用随机垂直裁剪，避免把结构删除后仍沿用“安全”标签。建议未来做小幅平移或尺度扰动时严格同步所有坐标；包内未实现这些额外增强。

步骤 B 使用相似组逆频率采样，避免单个近重复序列支配梯度。Graf 仅用于分层审查，不作路由，也不按类型极端过采样。质控与安全稀缺标签应优先补齐；没有标签时禁止假造。如果出现某质量类别数量极少，先补标并检查召回率，再考虑限幅重采样或训练集估计的类别权重；当前 BCE 未加类别权重。

主动学习优先：高上下边界分歧；质控边界附近；训练特征距离大；新界面样式；安全包络预测与裁剪不一致；人工大幅修正或曾发生误裁。先去近重复，再按特征聚类选有代表性的样本，避免一轮全选同一序列。建议每轮 50～100 张，抽一部分低不确定性样本做随机审计，发现模型“自信错误”。

修正数据只加入下一版训练池；冻结测试集不回流，冻结校准集也不能反复用于调参。新版本重新保存数据清单哈希、模型与校准文件。主动学习的选择器和多轮调度未自动化，当前代码接收人工更新后的 JSONL。

### 7.3 不按Graf拆模型

总样本只有 1000～2000 张，按类型拆分会稀释样本，罕见类型尤其容易过拟合；路由错误还会传播为裁剪错误。默认所有类型共享主干与裁剪头，训练无需 Graf 标签。分型信息存在时仅做分组评估。若要证明多模型有价值，必须在相同组划分、相同总训练预算下对照，并同时报告真分型路由和预测分型路由；前者只是理想上限。

## 8 评价与消融实验

### 8.1 必须同时报告的结果

| 指标 | 定义或注意事项 |
| --- | --- |
| 上下边界 MAE | 分别报告原图像素与除以真窗口高度后的误差 |
| 区间 IoU | 只评纵向；另报完整二维裁剪框 IoU 以暴露 A 的横向错误 |
| 容差合格率 | 上下误差均不超过 0.02H；A 失败计不合格 |
| 必保范围保留率 | 输出完整包含专家 safe_box 的比例；包络包含保证该包络内全部结构保留 |
| 严重误裁率 | 在已输出建议且有 safe 标签的图中，未完整包含 safe_box 的比例 |
| A 失败率 | 无唯一合格窗口等失败，不从总样本中删去 |
| 人工修改率 | 对当前版本真实人工复核结果计数，不能用误差替代 |
| 快速确认覆盖率 | 通过全部筛查的比例；仍需人工确认 |
| 筛查后安全合格率 | 通过筛查者中 quality=1 且完整保留 safe_box 的比例 |
| 分组结果 | Graf、质量、界面样式；未知单列并报告样本数 |

所有指标注明分母和缺失标签数。边界 MAE 只在能输出预测且有有效裁剪标签的图上计算，因此必须紧邻报告 A 失败率；不能用较低条件 MAE 掩盖大量拒绝。代码把关键结构保留的端到端失败与已产生裁剪的严重误裁分别列出。

无 safe_box 就不填“结构保留率 100%”。没有实际人工修改记录时，human_modified 为 null；edit_proxy_rate 只表示超过工程容差的比例，不等于真实人工修改率。比较模型时固定边距，另报裁剪高度或保留面积比例，防止模型靠保留整幅窗口得到虚假的安全优势。

建议按 leak_group 做 bootstrap 置信区间，并报告每组最坏结果；由于组不是患者，这仍不是患者级置信区间。当前评估代码输出逐图结果与分组均值，不自动计算 bootstrap，研究报告应另行基于输出做统计。

### 8.2 最小消融矩阵

1. 固定训练集统计边界、全局池化回归基线、推荐空间多任务模型。
2. 相同真窗口输入下的有序坐标回归与单类别目标检测裁剪头；比较数据效率与纵向误差，检测框也约束到全宽评估。
3. 推荐空间头仅裁剪、加安全包络、加质控、二者都加；区分空间结构收益与辅助任务收益。
4. 统一模型与 Graf 分模型；没有足够可靠分型时标为暂不可做，不用模型伪标签冒充真实类型。
5. 同一模型有无 0.02H 安全边距；同时看误裁和面积。
6. 常规抽样与困难样本补标、主动学习补标；每组新增标注预算相同。
7. 随机图片划分与审核后相似组划分。只用于展示已知近重复泄漏的影响；不能将它伪称“随机与患者级划分”的比较。
8. 真实窗口和预测窗口；已见样式与留样式外测；分别量化 A 误差传播和域偏移。

第 7 项与原要求的差别必须保留：没有患者信息，无法完成严格患者划分消融。以后增加有可追踪编号的新数据，再补患者级比较。

如何排除固定位置捷径：对正确窗口做保留完整结构的小幅位置扰动并同步变换标签，检查预测是否随解剖移动；比较固定裁剪；在相似组外和新样式上测试；训练前排查测量线、角度文字是否只存在于特定标签。显著性图只能辅助找问题，不能证明模型已经理解解剖。

随包实现推荐双阶段模型、全局回归基线、真实与预测窗口评估、分组指标和合成测试。检测裁剪头、Graf 多专家、自动主动学习调度、bootstrap 以及全部消融批量执行不在代码中，不把实验设计冒充已完成结果。

## 9 失败模式与拒绝机制

### 9.1 三类不确定性分别处理

模型分歧：仅让裁剪头 Dropout 开启，主干 BatchNorm 固定，前向 16 次，取 top 和 bottom 的标准差最大值。它只是边界稳定性的排序信号；一个模型可能在分布外稳定地犯错，不能把 1−std 当安全概率。

切面可用性：质控 logit 用 val 的正负标签拟合一个 temperature，sigmoid(logit/T) 阈值初值为 0.9。温度缩放只校准这个二分类输出，不校准边界误差，也不证明临床安全。[4]

域偏移：用 train 的 L2 归一化 512 维特征建立参考库，计算最近特征的欧氏距离。未知或未见过的 style_group 强制人工处理；已知外观中仍用距离筛查。该距离也不是可靠的通用 OOD 检测器。

### 9.2 阈值选择和独立核查

val 上的可用且有安全标注样本确定分歧和特征距离的第 95 百分位阈值。检测分数 0.8、质量阈值 0.9、额外边距 0.02 是初值；如需调优，仅在 val 预先搜索并冻结。在独立 calib 上只核查这套固定规则，不能一边看失败一边反复调阈值。

calib 中通过筛查的图必须有安全和质控真值。同一 leak_group 中，只要任一通过筛查的图发生关键结构误裁或 quality=0，该组记一次失败。代码计算组失败概率的单侧 95% Clopper–Pearson 上界；默认至少 30 个合格组且上界不超过 5% 才标记可进入快速确认。

$$k=0:\qquad U_{95}=1-0.05^{1/n}$$

因此 30 组零失败也不足以过 5% 这条门槛；至少 59 个零失败组才可以。若要求上界小于 1%，零失败也至少需要 299 组。5% 只是演示性的研究门槛，不是医学可接受标准。现有小数据可能始终不足，这时程序保持拒绝快速确认，不降低门槛制造结果。

由于患者独立性未知，二项分布的独立性假设未获验证；这里的上界只能称名义统计核查，不能当临床保证。即使通过，输出仍固定 review_required=true。程序不会自动批准或覆盖原图。

### 9.3 具体回退

任何无窗口、多窗口、未知样式、质控不足、边界分歧大、特征异常、校准不足、辅助头未训练，都进入人工复核；若没有合理窗口，则不生成裁剪图。A 若把结构裁掉，B 不能复原，所以要单独检查窗口包含安全包络的比例。

新设备或新医院数据先独立成批审查，补窗口、安全包络和质控标签，使用旧数据混合微调防止遗忘，再重新校准。仅分辨率变化由坐标归一化和等比缩放处理；扫描深度、探头频率、动态范围、叠字、非标准切面变化不一定能靠归一化解决。

严禁自动把被拒绝的样本当负训练标签，也不把模型自己的裁剪当人工真值。不能识别内部刻度、测量线是否影响学习时，先做有叠字与无叠字的分层评估；本包不进行擦除文字或生成式修复，以免改变解剖信号。

## 10 PyTorch核心代码

### 10.1 文件和实现范围

ddh.py 包含 JSONL Dataset、增强、两阶段网络、有序裁剪头、mask 损失、训练与验证、最佳权重保存加载、MC Dropout、温度拟合、校准门控、近重复分组、分组评估和单图推理。关键计算是真实 PyTorch 实现，不依赖占位伪代码。

requirements.txt 指定 torch 2.5.1 与 torchvision 0.20.1 的兼容组合。建议 Python 3.11；CPU 与 CUDA 轮子按本机环境选择。README.md 给出安装和完整运行命令。annotations.example.jsonl 是字段示例，引用的图片不存在，必须替换为自己的数据。

### 10.2 坐标转换全过程

第一步：A 在原截图上检测出 xyxy，向外取整并夹到原图边界。内部缩放的还原由检测器完成。

第二步：用该框提取窗口。记录窗口宽高；等比例缩放加 letterbox。roi 存的是真实内容在 384 画布内的四边，模型用 ROIAlign 移除填充区域的直接位置影响。

第三步：B 输出的 bounds 已定义为原窗口内的归一化纵向比例，不是 384 画布上的比例。因此不要再次对 bounds 减 padding；那会造成二次坐标变换错误。

第四步：在归一化 top 减去校准边距，在 bottom 加上边距，截断到 [0,1]，再乘检测窗口高度，加检测窗口原图 y₀。top 向下取整，bottom 向上取整，左右直接用 A 的 x₀ 和 x₁。raw_crop 函数完成这一步。

第五步：输出原图 crop_box 与拒绝原因。--preview 只写一份未批准候选图；没有人工确认步骤，不作为最终标注真值。程序拒绝用预览路径覆盖原始图像。

### 10.3 验证边界和下一步

tests/test_core.py 测试极端 logits 仍有序、等比填充 ROI、坐标还原、IoU、名义二项上界、缺失标签、辅助头梯度、MC 状态恢复、检查点读写、重复泄漏阻断、校准策略核查和默认拒绝。tests/smoke_train.py 用随机纹理测试两个训练循环与推理路径，不下载预训练权重。

合成测试不能评价 DDH 解剖结构保留。下一步不是继续扩大模型，而是完成标注规则试点，建立审核后的分组清单，先跑基线，再用相同数据划分评价安全辅助头是否真的降低严重误裁。训练日志、检查点、校准 JSON 和测试逐图输出需按版本保留。

### 10.4 一手参考资料

[1] Torchvision 0.20 Faster R-CNN MobileNetV3 Large FPN 官方接口。https://docs.pytorch.org/vision/0.20/models/generated/torchvision.models.detection.fasterrcnn_mobilenet_v3_large_fpn.html

[2] PyTorch 官方目标检测微调教程。https://docs.pytorch.org/tutorials/intermediate/torchvision_tutorial.html

[3] Joint Landmark and Structure Learning for Automatic Evaluation of Developmental Dysplasia of the Hip。用于说明结构监督与关键点任务的相关研究背景，不代表本方案复现了该论文。https://arxiv.org/abs/2106.05458

[4] On Calibration of Modern Neural Networks。温度缩放方法来源；不提供本任务裁剪安全保证。https://arxiv.org/abs/1706.04599

本方案的网络组合、标签预算、损失权重、输入尺寸与阈值是针对当前约束提出的工程设计，尚未在这批真实数据上验证，不能把文献中的效果转移为本项目结果。
