# 代码验证记录

日期：2026-09-21。范围：研究代码的运行与关键约束，不是医学性能验证。

## 测试环境

- Python 3.12.14，Linux，CPU。
- torch 2.5.1+cpu，torchvision 0.20.1+cpu。
- numpy 2.5.2，Pillow 12.3.0。
- 无 DDH 训练数据；没有下载预训练权重参与本次测试。

## 已通过的12个核心测试

1. 极端 logits 下 top < bottom，并可反向传播。
2. 原截图坐标转换以及向外取整。
3. 非正方形窗口的等比缩放和 padding ROI。
4. 纵向区间 IoU 的相同与不重叠情况。
5. 零失败二项上界，59 组与 30 组对应的门槛差异。
6. 推荐模型输出形状、三个任务头梯度、MC Dropout 状态恢复和检查点往返加载。
7. 完全缺失监督标签时的零损失处理。
8. 多个高分窗口时拒绝。
9. 来源分组、审核后划分及跨组泄漏拦截。
10. 不同 leak_group 下相同解码图片跨集合时仍被拦截。
11. 未校准、未知界面样式时默认人工处理。
12. 固定合成预测下的校准策略：60 组零失败可通过研究门槛；出现未标注的通过筛查样本时禁止启用。

执行结果：Ran 12 tests，OK。第12项替换模型预测，测试门控统计逻辑；不代表真实模型有60组零失败。

## 双阶段烟雾测试

由 tests/smoke_train.py 生成16张随机纹理图，原图尺寸120×100，窗口80×80。按预先独立的合成组分为8张train、4张val、2张calib、2张test；每个模型用随机初始化、CPU、输入短边或画布96、batch 2，训练1 epoch。

已经跑通：步骤 A 的检测训练与验证、步骤 B 的多任务训练与验证、最佳检查点保存和重新加载、端到端评估、人工真窗口评估、单图推理。

检测器没有在合成测试图上找到符合阈值的唯一窗口，程序正确返回 missing_or_multiple_windows，review_required=true，没有生成伪装成功的裁剪。这是预期的安全回退验证，不是模型准确率结果。

## 尚未验证

- 真实 DDH 数据上的误差、结构保留率与人工修改率。
- 患者级独立性、跨医院泛化及未见设备安全性。
- GPU 显存和速度，正式预训练初始化的完整训练收敛。
- 文档提出的全部消融实验与主动学习收益。

请不要把合成测试产生的权重用于真实数据。压缩包不含这些权重或合成输出，避免与可用模型混淆。
